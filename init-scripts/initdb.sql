CREATE TABLE IF NOT EXISTS "users" (
	"id" INTEGER NOT NULL,
	"role" VARCHAR(255),
	"phone" VARCHAR(255) NOT NULL,
	"email" VARCHAR(255) NOT NULL UNIQUE,
	"full_name" VARCHAR(255),
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP NOT NULL,
	PRIMARY KEY("id")
);




CREATE TABLE IF NOT EXISTS "orders" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"user_id" INTEGER NOT NULL,
	"capacity" INTEGER NOT NULL,
	"lifting_capacity" INTEGER NOT NULL,
	"type" INTEGER,
	"manager_id" INTEGER,
	"price" DECIMAL,
	"status" VARCHAR(255) NOT NULL DEFAULT '1',
	"pickup_address" VARCHAR(255),
	"delivery_address" VARCHAR(255),
	"date_start" DATE,
	"date_end" DATE,
	"period_from" DATE,
	"period_to" DATE,
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP,
	PRIMARY KEY("id")
);


CREATE INDEX "orders_index_status"
ON "orders" ("status");
CREATE INDEX "orders_index_1"
ON "orders" ("user_id", "manager_id", "capacity", "lifting_capacity", "status");

CREATE TABLE IF NOT EXISTS "drivers" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"status" INTEGER NOT NULL DEFAULT 0,
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP NOT NULL,
	PRIMARY KEY("id")
);


CREATE INDEX "driver_index_status"
ON "drivers" ("status");

CREATE TABLE IF NOT EXISTS "autos" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"status" INTEGER NOT NULL DEFAULT 0,
	"capacity" INTEGER NOT NULL,
	"lifting_capacity" INTEGER NOT NULL,
	"number" VARCHAR(10) NOT NULL,
	"required_category" VARCHAR(10) NOT NULL,
	"created_at" TIMESTAMP,
	"update_at" TIMESTAMP,
	PRIMARY KEY("id")
);


CREATE INDEX "auto_index_capacity"
ON "autos" ("capacity");
CREATE INDEX "auto_index_lifting_capacity"
ON "autos" ("lifting_capacity");
CREATE INDEX "auto_index_2"
ON "autos" ("status");
CREATE INDEX "auto_index_3"
ON "autos" ("required_category");

CREATE TABLE IF NOT EXISTS "driver_auto" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"driver_id" INTEGER,
	"order_id" INTEGER,
	"auto_id" INTEGER,
	"status" INTEGER NOT NULL DEFAULT 0,
	"date_finish" DATE,
	"created_at" TIMESTAMP,
	"updated_at" TIMESTAMP,
	PRIMARY KEY("id")
);


CREATE INDEX "driver_auto_index_0"
ON "driver_auto" ("driver_id");
CREATE INDEX "driver_auto_index_1"
ON "driver_auto" ("order_id");
CREATE INDEX "driver_auto_index_2"
ON "driver_auto" ("auto_id");

CREATE TABLE IF NOT EXISTS "driver_categories" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"driver_id" INTEGER NOT NULL,
	"category" VARCHAR(10) NOT NULL,
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP,
	PRIMARY KEY("id")
);


CREATE INDEX "driver_category_index_0"
ON "driver_categories" ("category");
CREATE INDEX "driver_category_index_1"
ON "driver_categories" ("driver_id");

CREATE TABLE IF NOT EXISTS "managers" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"status" INTEGER,
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP NOT NULL,
	PRIMARY KEY("id")
);


CREATE INDEX "managers_index_status"
ON "managers" ("status");

CREATE TABLE IF NOT EXISTS "auths" (
	"id" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"login" VARCHAR(255) NOT NULL,
	"email" VARCHAR(255) NOT NULL,
	"password" VARCHAR(255) NOT NULL,
	"created_at" TIMESTAMP NOT NULL,
	"updated_at" TIMESTAMP,
	PRIMARY KEY("id")
);



ALTER TABLE "driver_auto"
ADD FOREIGN KEY("driver_id") REFERENCES "drivers"("id")
ON UPDATE NO ACTION ON DELETE NO ACTION;
ALTER TABLE "driver_auto"
ADD FOREIGN KEY("auto_id") REFERENCES "autos"("id")
ON UPDATE NO ACTION ON DELETE NO ACTION;
ALTER TABLE "driver_categories"
ADD FOREIGN KEY("driver_id") REFERENCES "drivers"("id")
ON UPDATE NO ACTION ON DELETE NO ACTION;